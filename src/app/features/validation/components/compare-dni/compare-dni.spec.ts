import { CompareDni } from './compare-dni';

describe('CompareDni placeholder', () => {
  it('should create component instance', () => {
    const c = new CompareDni();
    expect(c).toBeTruthy();
  });
});
